// Déclaration variables de type chaîne de caractères

let chaine1 = "Bonjour";
let chaine2 = "le monde";

// Fonction qui concatène deux chaînes de caractères
function concatener(str1, str2) {
  return str1 + " " + str2;
}

let resultat = concatener(chaine1, chaine2);
console.log(resultat);
